# Singularity_Go_2 正式研发计划

># Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

### Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalO# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

-# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
-# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSock# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + Person# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + Cost# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
|# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌────────────────────────────────────────────────────────# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebR# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • Voxel# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • Navigation# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Uni# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└────────────────# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phas# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help`# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test`# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2`# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCK# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | C# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
|# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
|# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceas# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
|# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/f# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 +# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：**# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
|# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop`# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop` 机器人实际停止 |
| 2.5 状态推送验证 | Ceaser | 20 min | WebSocket 推送真实机器人状态 |
| 2.6 网络稳定性测试 |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop` 机器人实际停止 |
| 2.5 状态推送验证 | Ceaser | 20 min | WebSocket 推送真实机器人状态 |
| 2.6 网络稳定性测试 | Ceaser | 20 min | 持续运行 5 分钟不掉线 |

**里程碑# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop` 机器人实际停止 |
| 2.5 状态推送验证 | Ceaser | 20 min | WebSocket 推送真实机器人状态 |
| 2.6 网络稳定性测试 | Ceaser | 20 min | 持续运行 5 分钟不掉线 |

**里程碑 M2：** Nero 从 Windows 端可访问所有 API 端点，看到真实 Go# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop` 机器人实际停止 |
| 2.5 状态推送验证 | Ceaser | 20 min | WebSocket 推送真实机器人状态 |
| 2.6 网络稳定性测试 | Ceaser | 20 min | 持续运行 5 分钟不掉线 |

**里程碑 M2：** Nero 从 Windows 端可访问所有 API 端点，看到真实 Go2 画面

---

### Phase 3: 智能跟随 + 房间扫描 (Day# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop` 机器人实际停止 |
| 2.5 状态推送验证 | Ceaser | 20 min | WebSocket 推送真实机器人状态 |
| 2.6 网络稳定性测试 | Ceaser | 20 min | 持续运行 5 分钟不掉线 |

**里程碑 M2：** Nero 从 Windows 端可访问所有 API 端点，看到真实 Go2 画面

---

### Phase 3: 智能跟随 + 房间扫描 (Day 1, 后续 3 小时)

**目标：** 人物跟随和房间扫描# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop` 机器人实际停止 |
| 2.5 状态推送验证 | Ceaser | 20 min | WebSocket 推送真实机器人状态 |
| 2.6 网络稳定性测试 | Ceaser | 20 min | 持续运行 5 分钟不掉线 |

**里程碑 M2：** Nero 从 Windows 端可访问所有 API 端点，看到真实 Go2 画面

---

### Phase 3: 智能跟随 + 房间扫描 (Day 1, 后续 3 小时)

**目标：** 人物跟随和房间扫描功能可正常工作

| 任务 | 负责人 | 预计时间 | 验收标准 |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop` 机器人实际停止 |
| 2.5 状态推送验证 | Ceaser | 20 min | WebSocket 推送真实机器人状态 |
| 2.6 网络稳定性测试 | Ceaser | 20 min | 持续运行 5 分钟不掉线 |

**里程碑 M2：** Nero 从 Windows 端可访问所有 API 端点，看到真实 Go2 画面

---

### Phase 3: 智能跟随 + 房间扫描 (Day 1, 后续 3 小时)

**目标：** 人物跟随和房间扫描功能可正常工作

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 3.1 PersonTracker 验证 |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop` 机器人实际停止 |
| 2.5 状态推送验证 | Ceaser | 20 min | WebSocket 推送真实机器人状态 |
| 2.6 网络稳定性测试 | Ceaser | 20 min | 持续运行 5 分钟不掉线 |

**里程碑 M2：** Nero 从 Windows 端可访问所有 API 端点，看到真实 Go2 画面

---

### Phase 3: 智能跟随 + 房间扫描 (Day 1, 后续 3 小时)

**目标：** 人物跟随和房间扫描功能可正常工作

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 3.1 PersonTracker 验证 | Ceaser | 30 min | 单人场景跟踪成功率 ≥ 80% |
|# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop` 机器人实际停止 |
| 2.5 状态推送验证 | Ceaser | 20 min | WebSocket 推送真实机器人状态 |
| 2.6 网络稳定性测试 | Ceaser | 20 min | 持续运行 5 分钟不掉线 |

**里程碑 M2：** Nero 从 Windows 端可访问所有 API 端点，看到真实 Go2 画面

---

### Phase 3: 智能跟随 + 房间扫描 (Day 1, 后续 3 小时)

**目标：** 人物跟随和房间扫描功能可正常工作

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 3.1 PersonTracker 验证 | Ceaser | 30 min | 单人场景跟踪成功率 ≥ 80% |
| 3.2 follow.start 实现 | Ceaser | 30 min | 机器人开始跟随# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop` 机器人实际停止 |
| 2.5 状态推送验证 | Ceaser | 20 min | WebSocket 推送真实机器人状态 |
| 2.6 网络稳定性测试 | Ceaser | 20 min | 持续运行 5 分钟不掉线 |

**里程碑 M2：** Nero 从 Windows 端可访问所有 API 端点，看到真实 Go2 画面

---

### Phase 3: 智能跟随 + 房间扫描 (Day 1, 后续 3 小时)

**目标：** 人物跟随和房间扫描功能可正常工作

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 3.1 PersonTracker 验证 | Ceaser | 30 min | 单人场景跟踪成功率 ≥ 80% |
| 3.2 follow.start 实现 | Ceaser | 30 min | 机器人开始跟随主要目标 |
| 3.3 follow.hold 实现 | Ceaser | 15 min# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop` 机器人实际停止 |
| 2.5 状态推送验证 | Ceaser | 20 min | WebSocket 推送真实机器人状态 |
| 2.6 网络稳定性测试 | Ceaser | 20 min | 持续运行 5 分钟不掉线 |

**里程碑 M2：** Nero 从 Windows 端可访问所有 API 端点，看到真实 Go2 画面

---

### Phase 3: 智能跟随 + 房间扫描 (Day 1, 后续 3 小时)

**目标：** 人物跟随和房间扫描功能可正常工作

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 3.1 PersonTracker 验证 | Ceaser | 30 min | 单人场景跟踪成功率 ≥ 80% |
| 3.2 follow.start 实现 | Ceaser | 30 min | 机器人开始跟随主要目标 |
| 3.3 follow.hold 实现 | Ceaser | 15 min | 机器人保持当前位置 |
| 3.4 scan.start 实现 | Ceaser | 30 min | 机器人开始自主探索 |
| 3.5 mission.stop 实现 |# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop` 机器人实际停止 |
| 2.5 状态推送验证 | Ceaser | 20 min | WebSocket 推送真实机器人状态 |
| 2.6 网络稳定性测试 | Ceaser | 20 min | 持续运行 5 分钟不掉线 |

**里程碑 M2：** Nero 从 Windows 端可访问所有 API 端点，看到真实 Go2 画面

---

### Phase 3: 智能跟随 + 房间扫描 (Day 1, 后续 3 小时)

**目标：** 人物跟随和房间扫描功能可正常工作

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 3.1 PersonTracker 验证 | Ceaser | 30 min | 单人场景跟踪成功率 ≥ 80% |
| 3.2 follow.start 实现 | Ceaser | 30 min | 机器人开始跟随主要目标 |
| 3.3 follow.hold 实现 | Ceaser | 15 min | 机器人保持当前位置 |
| 3.4 scan.start 实现 | Ceaser | 30 min | 机器人开始自主探索 |
| 3.5 mission.stop 实现 | Ceaser | 15 min | 停止所有任务 |
| 3.6 多人# Singularity_Go_2 正式研发计划

> 版本: 1.0 | 日期: 2026-07-24 | 作者: Ceaser
> 
> 基于 DimOS 官方文档、Nero 工作流契约、以及四份 Roadmap 文档的综合产物

---

## 1. 项目概述

### 1.1 目标

基于 DimensionalOS (DimOS) 构建 Unitree Go2 机器人的智能控制系统，通过 HTTP/WebSocket API 桥接至 Nero 的 Dolly 系统，实现：

- 机器人视频流实时传输至 OBS
- 房间自主扫描与建图
- 人体检测与智能跟随
- 语音/聊天指令远程控制
- 多层安全保护机制

### 1.2 核心指标

| 指标 | 目标值 | 验证方式 |
|------|--------|---------|
| 视频帧率 | ≥ 10 fps | `benchmark_frames.py` |
| 视频分辨率 | ≥ 640x480 | PIL 读取验证 |
| 端到端延迟 | < 500ms | frame timestamp 差值 |
| 人物跟踪成功率 | ≥ 80% | 室内多人场景测试 |
| 安全层测试覆盖率 | 100% | pytest --cov |
| API 可用性 | 99% (心跳间隔 200ms) | WebSocket 监控 |

### 1.3 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 机器人 OS | DimOS (DimensionalOS) | 开源机器人操作系统 |
| 机器人通信 | WebRTC | 通过 GO2Connection 模块 |
| 感知 | YOLO11-pose + PersonTracker | 人体检测与跟踪 |
| 导航 | VoxelGridMapper + CostMapper + A* | SLAM 建图与路径规划 |
| API 桥接 | FastAPI + uvicorn | 嵌入 DimOS Module 生命周期 |
| 实时通信 | WebSocket + MJPEG | 状态推送 + 视频流 |
| 包管理 | uv | Python 依赖管理 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  Nero Dolly (Windows)                                   │
│  RobotGatewayClient → HTTP/WS → Ceaser robot-service    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Ceaser Linux (robot-service)                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Custom Blueprint: singularity-go2                │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  DimOS 内置模块 (80%)                        │  │  │
│  │  │  • GO2Connection (WebRTC)                    │  │  │
│  │  │  • VoxelGridMapper + CostMapper + A*         │  │  │
│  │  │  • Detection2D + PersonTracker               │  │  │
│  │  │  • PersonFollowSkillContainer                │  │  │
│  │  │  • NavigationSkillContainer                  │  │  │
│  │  │  • SpatialMemory                             │  │  │
│  │  │  • McpServer + McpClient                     │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │  自定义模块 (20%)                             │  │  │
│  │  │  • DollyGatewayModule (HTTP/WS on :8780)    │  │  │
│  │  │  • SingularitySkillContainer (@skill)        │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  Unitree Go2 (WebRTC, 无需 jailbreak)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 阶段划分与里程碑

### Phase 0: 环境准备 (Day 0, 前 2 小时)

**目标：** DimOS 在 Linux 上可运行，replay 模式验证通过

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 0.1 系统依赖安装 | Ceaser | 15 min | curl, g++, git-lfs, libturbojpeg 等安装成功 |
| 0.2 uv 安装 | Ceaser | 5 min | `uv --version` 正常输出 |
| 0.3 DimOS 克隆 + 安装 | Ceaser | 30 min | `dimos --help` 正常输出 |
| 0.4 Replay 验证 | Ceaser | 15 min | Rerun 窗口显示相机画面和 SLAM 地图 |
| 0.5 FastAPI 嵌入验证 | Ceaser | 30 min | `GET /test` 返回 `{"status": "ok"}` |
| 0.6 硬件门禁测试 | Ceaser | 30 min | 输出 Nero 要求的门禁报告 |

**里程碑 M0：** `dimos --replay run unitree-go2` 成功运行，截图发送给 Nero

**门禁报告输出格式：**
```
MODEL:        [Air/Pro/EDU]
SDK INIT:     [WebRTC 连接成功/失败]
FRONT CAMERA: [Rerun 中可见/不可见]
STATE:        [odometry 可读/不可读]
MOVE:         [navigation goal 成功/失败]
STOP:         [dimos stop 生效/不生效]
OBSTACLE DATA:[LiDAR 点云可用/不可用]
BLOCKER:      [阻塞项]
```

---

### Phase 1: 安全层 + API 骨架 (Day 0, 后续 4 小时)

**目标：** 安全层 100% 测试覆盖，所有 API 端点可响应

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 1.1 TTL 过期拒绝 | Ceaser | 30 min | 过期命令返回 `reason: "ttl_expired"` |
| 1.2 心跳看门狗 | Ceaser | 30 min | 1.5s 无心跳触发 estop |
| 1.3 过期帧检测 | Ceaser | 20 min | 帧 > 500ms 返回 503 |
| 1.4 紧急停止优先级 | Ceaser | 20 min | estop 后所有命令被拒绝 |
| 1.5 安全层单元测试 | Ceaser | 40 min | 5 个测试全部通过，覆盖率 100% |
| 1.6 GET /v1/health | Ceaser | 15 min | 返回 `{"status": "ok", ...}` |
| 1.7 GET /v1/state | Ceaser | 30 min | 映射 DimOS 状态为 Nero 契约格式 |
| 1.8 POST /v1/stop | Ceaser | 20 min | 返回 `{"executed": true, ...}` |
| 1.9 WS /v1/events | Ceaser | 45 min | 每 200ms 推送一则状态消息 |
| 1.10 GET /v1/frame.jpg | Ceaser | 30 min | 返回有效 JPEG 图像 |
| 1.11 GET /v1/video.mjpeg | Ceaser | 30 min | 持续推送 MJPEG 流 |
| 1.12 POST /v1/commands | Ceaser | 45 min | 命令队列 + 去重 + 路由 |

**里程碑 M1：** 所有 7 个 API 端点返回正确响应，安全层 5 个测试全部通过

---

### Phase 2: 硬件集成 (Day 1, 前 2 小时)

**目标：** 连接真实 Go2，验证完整视频链路和控制链路

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 2.1 双网卡配置 | Ceaser | 20 min | eth0 → 团队 LAN, eth1 → 机器人 |
| 2.2 连接真实 Go2 | Ceaser | 15 min | `dimos run unitree-go2` 成功启动 |
| 2.3 视频链路验证 | Ceaser | 30 min | Nero 端可看到真实 Go2 画面 |
| 2.4 控制链路验证 | Ceaser | 15 min | `POST /v1/stop` 机器人实际停止 |
| 2.5 状态推送验证 | Ceaser | 20 min | WebSocket 推送真实机器人状态 |
| 2.6 网络稳定性测试 | Ceaser | 20 min | 持续运行 5 分钟不掉线 |

**里程碑 M2：** Nero 从 Windows 端可访问所有 API 端点，看到真实 Go2 画面

---

### Phase 3: 智能跟随 + 房间扫描 (Day 1, 后续 3 小时)

**目标：** 人物跟随和房间扫描功能可正常工作

| 任务 | 负责人 | 预计时间 | 验收标准 |
|------|--------|---------|---------|
| 3.1 PersonTracker 验证 | Ceaser | 30 min | 单人场景跟踪成功率 ≥ 80% |
| 3.2 follow.start 实现 | Ceaser | 30 min | 机器人开始跟随主要目标 |
| 3.3 follow.hold 实现 | Ceaser | 15 min | 机器人保持当前位置 |
| 3.4 scan.start 实现 | Ceaser | 30 min | 机器人开始自主探索 |
| 3.5 mission.stop 实现 | Ceaser | 15 min | 停止所有任务 |
| 3.6 多人场景测试 | Ceaser | 30 min | 3-5 人场景中正确